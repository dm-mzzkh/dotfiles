return {
  "jackplus-xyz/player-one.nvim",
  enabled = false,
  opts = {
    min_interval = 0.1, -- increase delay between sounds to 100ms
    theme = "chiptune",    -- use the synthesizer sound theme
  }
}
